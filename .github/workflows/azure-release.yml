name: Release to Azure

on:
  push:
    branches: [ princess ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.401
    - name: Install dependencies
      run: dotnet restore
    - name: Install libman and signalr
      run: |
        cd Server
        dotnet tool restore
        dotnet libman restore
        # fail if libman couldn't download the files.
        if [[ -f wwwroot/js/dist/browser/signalr.js && -f wwwroot/js/dist/browser/signalr.min.js ]]; then
          exit 0;
        else
          exit 1;
        fi
    - name: Ahead-of-time compress static assets
      run: |
        cd Server
        gzip -k -9 wwwroot/js/*
        gzip -k -9 wwwroot/js/dist/browser/*
        gzip -k -9 wwwroot/css/*
        gzip -k -9 wwwroot/*.xml
        gzip -k -9 wwwroot/*.webmanifest
        gzip -k -9 wwwroot/*.svg
    - name: Publish
      run: dotnet publish --configuration Release --no-restore -o built
    - name: Azure Login
      uses: Azure/login@v1
      with:
        # Paste output of `az ad sp create-for-rbac` as value of secret variable: AZURE_CREDENTIALS
        creds: ${{secrets.AZURE_CREDENTIALS}}
    - name: Azure WebApp
      uses: Azure/webapps-deploy@v2
      with:
        # Name of the Azure Web App
        app-name: BuzzOffDev
        # Applies to Web App only: Path to package or folder. *.zip, *.war, *.jar or a folder to deploy
        package: built
        # Enter the start up command. For ex. dotnet run or dotnet run
        startup-command: dotnet BuzzOff.Server.dll

